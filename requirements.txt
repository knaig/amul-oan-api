## basic setup packages
wheel
setuptools
python-dotenv==1.2.1
httpx-oauth==0.16.1
Jinja2==3.1.6  # For template rendering

# for data/DB
pandas==2.3.3
simplejson==3.20.2

### for DB
pg8000==1.31.5
sqlalchemy==2.0.46
requests==2.32.5

# LLM
openai==2.16.0
anthropic==0.77.0
pydantic==2.12.5
tiktoken==0.12.0
tenacity==9.1.2
slowapi==0.1.9

# Langfuse
langfuse
gevent>=22.10.0

# Prometheus metrics (optional at runtime; app/metrics.py degrades to a no-op when absent)
prometheus-client==0.21.1

# Redis for caching and stuff
redis==7.1.0

# Settings
python-decouple==3.8
hvac>=2.3.0,<3.0.0

openpyxl==3.1.5

### Pydantic AI (Updated to resolve conflicts)
pydantic-ai-slim[ag-ui,anthropic,bedrock,cli,cohere,evals,fastmcp,google,groq,huggingface,logfire,mcp,openai,retries,temporal,ui,vertexai,xai]==1.50.0
asgiref==3.11.0

# Web Server
gunicorn==24.1.1
uvicorn[standard]==0.40.0  # ASGI server
uvicorn-worker==0.4.0

# FastAPI and dependencies
fastapi[standard]==0.128.0
aioredis==2.0.1
starlette==0.50.0
httpx==0.28.1
apscheduler==3.10.4
pymupdf==1.28.0

# Search and Data
rapidfuzz==3.14.3
regex

# Authentication and Security
PyJWT==2.10.1
cryptography==46.0.4
firebase-admin>=6.0.0

# Async utilities
aiohttp>=3.9.0
aiofiles==25.1.0
aiocache==0.12.3
pydantic-settings==2.12.0
python-dateutil==2.9.0.post0

# Audio conversion (WebM/Opus -> WAV for Bhashini; requires ffmpeg installed on system)
pydub>=0.25.1

# Language codes
langcodes[data]==3.5.1

# Humanize time, date, numbers, etc.
humanize==4.15.0
asyncpg==0.30.0
greenlet==3.1.1

# TypeSafe (Jev) planner + side-by-side trace store
typesafe-sdk>=0.7.1
aiosqlite>=0.20.0
